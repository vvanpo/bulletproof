package bp

// Scan the instance files listed in .bp/global and .bp/local, and verify
// their consistency with the object store.  If the object store is
// inconsistent, update the store.
func (s *Session) verifyObjects() {
	return
}
